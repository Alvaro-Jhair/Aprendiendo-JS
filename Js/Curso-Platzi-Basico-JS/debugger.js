function restarDos(num) {
    return num - 2;
}
function calcular() {
    const sumaTotal = 4 + 5;
    return restarDos(sumaTotal);
}

//debugger es una instruccion que permite hacer debug en nuestro programa
debugger;
calcular();
